I'm new to git and the command line. Explain what a command does before running it, especially anything that deletes, moves, or rewrites history. Don't assume I know shell conventions.

I'm on macOS Intel. Homebrew no longer ships prebuilt bottles for this platform, so brew install compiles from source and is slow — prefer a project's prebuilt release binary when one exists.